# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
build/
dist/

# Misc
.DS_Store

# Environment variables
.env
# Firebase service-account keys must NEVER be committed (backend reads the
# JSON from the FIREBASE_SERVICE_ACCOUNT_KEY env var, not from a file)
*-firebase-adminsdk-*.json
serviceAccount*.json
firebase-service-account*.json
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc

# Downloaded test data (reproducible from PlantVillage GitHub)
ml-service/test_images/plantvillage_color/

# Backend runtime artifacts
backend/logs/
backend/.data/

# Local tool state (Freebuff)
.freebuff/

# IDE specific files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ML training workspace (large dataset zip + venvs — never commit)
backend/scripts/ml-work/
.venv-ml/
ml-pip.log
ml-dl.log

# Local reference material & scratch (not part of the product)
Frontend-kisan-360/
.trae/
Kisan360_HLD (2).pdf
*.log

# Office lock files (deck open in PowerPoint while git status runs)
~$*.pptx
~$*.docx

# UI-audit harness ephemera — versioned: the .cjs probe scripts only.
# Never commit: installed browser driver, chrome profile, screenshots, run results.
.harness/node_modules/
.harness/chrome-profile/
.harness/chrome-profile*/
.harness/shots/
.harness/*.json
.harness/.tok

# Scratch curl output from harness runs (holds a demo JWT)
/demo.json
